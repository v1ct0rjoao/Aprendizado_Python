# 2. Tuplas (Imutáveis e Ordenadas)

# Tuplas são semelhantes às listas, mas não podem ser modificadas após a criação. Isso torna as tuplas úteis para armazenar dados que não devem mudar.

cores = ("vermelho", "azul", "verde")
print(cores[1])